apiVersion: v1
kind: Service
metadata:
  name: etnair-api
spec:
  type: NodePort
  selector:
    app: etnair-api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32000     # URL de test : http://172.16.250.14:32000

